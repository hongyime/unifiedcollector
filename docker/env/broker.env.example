# broker.env.example — per-service scoped env template.
# Scope: rabbitmq, redis, collector_whatsapp, postgres.
# Rotation: values here can be rotated without touching other services' env files.
# Keys populated in step 5 of docs/plans/per-service-env-split.md.
# Copy to broker.env and fill in values. Wiring into compose is deferred; see docker/env/README.md.
