# Credentials (NEVER commit)
.env
!.env.example
credentials/

# Local scratch / one-off audit scripts (kept on disk, not tracked)
tmp/
scratch.py

# Large model files
models/dlib/*.dat
models/dlib/*.bz2

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
*.egg/

# Sessions
*.session
sessions/

# Runtime data
data/
*.db
*.db-wal
*.db-shm
storage/

# Node
node_modules/
*.log

# Build outputs
dist/
build/
.vite/

# Docker
*.tar
*.tar.gz

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Database dumps
*.dump
client_secret.json
*.pickle
.env.bak.*
scripts/_beeper_msgs.json
scripts/beeper_openapi.json

# Downloaded media (lives on docker named volume; host copy is throwaway)
media/

# Scratch / one-off fix scripts and runbooks (session artifacts)
fix_*.py
fix_*.cmd
*_RUNBOOK.md
URGENT_*.md
migrations/_tmp*.sql
/backups/
/sidecars/
/raw/
/manifests/
/queues/

# WhatsApp Baileys session credentials - never commit
# (sessions now consolidated under sessions/whatsapp/ alongside other platforms)
sessions/whatsapp/
sessions/*.session
# stray standalone-project copies that must never be re-added to the repo root
/whatsappcollector/
/whatsapp_session/
/telegramcollector/

templates/
.env.*
.venv/
venv/
.next/
coverage/



# AI / editor dot directories (managed via sourcerepo)
.*
!.github/
!.agents/
.agents/*
!.agents/STATE.md
!.agents/JOURNAL.md
!.agents/handoffs/
!.agents/handoffs/**
!.gitignore
!.gitattributes
!.gitmodules
!.editorconfig
!.nvmrc
!.node-version
!.python-version
!.tool-versions
!.prettierrc*
!.eslintrc*
!.stylelintrc*
!.babelrc*
!.browserslistrc
!.dockerignore
!.npmrc
!.yarnrc
!.yarnrc.yml
!.env.example
!.env.template
!.env.sample
!.sourcery.yml
!.deepsource.toml
skills/
skills-lock.json
docs/
# End AI / editor dot directories (managed via sourcerepo)
