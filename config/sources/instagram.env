# instagram.env  -- per-source tunable args (Option A)
# FAMOUS-FILTER: instagram already honors FILTER_MAX_FOLLOWERS at collect time
# (collector skips users above this). Bryan: cap at 1000 followers.
FILTER_MAX_FOLLOWERS=1000
INSTA_SPIDER_MAX_FOLLOWERS=1000
