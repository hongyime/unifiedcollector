# github.targets  -- file-authoritative target list (Option A)
# GitHub users (username) or repos (owner/repo). The FAMOUS-FILTER
# (GITHUB_FAMOUS_STAR_CAP in github.env, 1000) OVERRIDES this seed: repos/users
# above the star cap are skipped at collect time. Famous demo repos
# (pytorch/pytorch, huggingface/transformers, torvalds, ...) removed.
# Format:  target_id | Display Name | priority
# Edit + restart to apply.

theprawnorganisation | theprawnorganisation | 10
